"use strict";

/**
 * Read the documentation (https://strapi.io/documentation/v3.x/concepts/controllers.html#core-controllers)
 * to customize this controller
 */

module.exports = {
  /* Does not work
  async find(context) {
    console.log(123);
    const products = await strapi.services.products.find(context.query);

    return products.map((product) => {
      return sanitizeEntity(product.title);
    });
  },
  */
};
